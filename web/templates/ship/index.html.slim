table.table.table-hover
  thead
    tr
      th = gettext "Name"
      th = gettext "Type"
      th = gettext "Price (CR)"
      th = gettext "Top Speed (m/s)"
      th = gettext "Boost Speed (m/s)"
      th = gettext "Shields"
      th = gettext "Armor"
      th = gettext "Hull Mass (T)"
      th = gettext "Pad Size"
  tbody
    = render_many(@ships, ShipDesigner.ShipView, "index_item.html", conn: @conn)
